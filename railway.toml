[build]
buildCommand = "cd backend && npm install && npx prisma generate --schema=./src/prisma/schema.prisma"

[deploy]
startCommand = "node backend/src/server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
